# Supabase Configuration
# ----------------------
# Option 1: For a DEPLOYED Supabase instance (e.g., cloud-hosted)
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_KEY=your-service-role-key-here (This is the SERVICE ROLE KEY, not the anon key)

# Option 2: For LOCAL Supabase development (using the 'supabase_docker' setup)
# Uncomment these and fill them with values from 'supabase_docker/.env' after running 'docker-compose up'
SUPABASE_URL=http://localhost:8000
SUPABASE_KEY=your-local-supabase-service-role-key-from-supabase_docker/.env
# Note: The SUPABASE_KEY for local development is typically found as SERVICE_ROLE_KEY in supabase_docker/.env

# Server Configuration
PORT=5000
FLASK_ENV=development

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# LLM Configuration
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
LOCAL_LLM_API_URL=http://localhost:8080  # For local LLM API (e.g., Ollama, LM Studio)

# LLM Default Settings
DEFAULT_LLM_PROVIDER=openai  # Options: openai, anthropic, local
DEFAULT_TEXT_MODEL=gpt-3.5-turbo-instruct  # For text generation
DEFAULT_CHAT_MODEL=gpt-3.5-turbo  # For chat completion
DEFAULT_EMBEDDING_MODEL=text-embedding-ada-002  # For embeddings 